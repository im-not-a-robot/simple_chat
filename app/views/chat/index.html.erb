<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <meta http-equiv='X-UA-Compatible' content='IE=edge'>
  <title>Simple Chat</title>
  <meta name='viewport' content='width=device-width, initial-scale=1'>
</head>
<body>
  <div id="container">
    <div id="title"><h3>Simple Chat</h3></div>
      <div class="all_chats">
        <% @chats.each do |chat| %>
          <div class="per_person_chat">
            <div class="avatar">
              <img class="avatar_img" src="avatar.png" alt="avatar">
            </div>
            
            <div class="element rounded">
              <div class="email"><%= chat.user.email %></div>
              <div class="message"><%= chat.message %></div>
              <div class="timestamp"><%= chat.created_at %></div>
            </div>
          </div>
        <% end %>
      </div>

      <%= form_with url: "/chat", method: :post do |form| %>
        <div id="send_text">
          <%= form.text_area :message, size: "50x4", placeholder: "Write your message here.." %>
        </div>
        
        <%= form.submit "Send", id: "btn_send", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</body>
</html>